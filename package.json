{
  "name": "athletetime",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --port 3000",
    "build": "node build.mjs",
    "deploy": "npm run build && wrangler pages deploy public --project-name athletetime"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.2",
    "hono": "^4.9.5",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}